## Plan: Add UX Animation Components to the Chat Tab

### Context
The Chat tab (full-screen view in AIChatPage.tsx, lines ~1300–1378) currently uses:
- **Empty state** (line 1108–1131): Old custom SVG spinner with `animate-spin-slow` CSS class (12 rotating lines, `spin 3s linear infinite`)
- **Thinking indicator** (lines 1353–1377): Mode-conditional — `ThinkingIndicator` for chat mode, `WorkingHeader + ThoughtBlock` for agent mode
- **Chat input border glow** (lines 1152–1164, 1386–1398): Inline `animate-[spin_4s_linear_infinite]` utility classes

The AI Code Editor tab (lines ~1630–1663) already has the target pattern:
- **Thinking indicator** (line 1659): Single `AgentActionSequence` component (combines StepPulse + "Working for Xs" timer + ToolCallCard with "Exploring context")
- **No empty state**: IDE tab always shows the layout

### Changes

#### 1. Replace Empty-State SVG Spinner with `SpinnerBlock` (AIChatPage.tsx, lines 1111–1131)
- Add `import { SpinnerBlock } from '../../components/chat/SpinnerBlock';`
- Replace the entire `<div className="w-24 h-24 ... animate-spin-slow">...</svg></div>` block with `<SpinnerBlock label="Ready…" size="lg" color="emerald" />`
- `SpinnerBlock` uses the 5-frame terminal spinner `['●','◐','◓','◑','◒']` at 80ms intervals, matching the CLI pattern

#### 2. Replace Chat Tab Thinking Indicator with `AgentActionSequence` (AIChatPage.tsx, lines 1366–1374)
- Replace the conditional `{mode === 'chat' ? <ThinkingIndicator/> : <WorkingHeader/><ThoughtBlock/>}` block with a single `<AgentActionSequence key="agent-action-sequence-1" />`
- This matches the AI Code Editor tab (line 1659) which already uses `AgentActionSequence`
- `AgentActionSequence` internally combines all three patterns: StepPulse (emerald pulsing dot), "Working for Xs" timer, and ToolCallCard ("Exploring context" with collapsible animation), all using `--mcode-*` CSS variables

#### 3. Replace Tool Call Status Spinners with `SpinnerBlock` (ChatMessage.tsx)
- Import `SpinnerBlock` in ChatMessage.tsx
- Replace any existing `Loader2`/`animate-spin` spinner in tool call status areas with `<SpinnerBlock label="Running…" size="sm" color="emerald" active={!!isRunning} />`

#### 4. Add `ReactionBurst` for Visual Feedback (AIChatPage.tsx)
- Import `ReactionBurst`
- Add a `<ReactionBurst>` element in the Chat tab's message area, triggered on user message send (CSS keyframe pop + 6-particle explosion)

#### 5. Unify Chat Input Border Glow (CSS extraction)
- All three input areas use inline `animate-[spin_4s_linear_infinite]` with inconsistent gradients (0deg vs 90deg, blue→green vs green→blue)
- Add a shared `mcode-input-glow` CSS class in index.css using `cubic-bezier(0.16,1,0.3,1)` easing per the 4-layer animation architecture
- Replace inline animate utility classes with the named class

#### 6. Remove Dead `.animate-spin-slow` CSS Class (index.css, line 225)
- After replacing the empty state spinner, `.animate-spin-slow` becomes unused dead code — remove it

### Files to Edit
| File | Changes |
|------|---------|
| `packages/web/src/components/pages/AIChatPage.tsx` | Import `SpinnerBlock` + `ReactionBurst`; replace empty-state SVG spinner; replace conditional thinking indicator with `AgentActionSequence`; replace inline `animate-[spin_4s_linear_infinite]` classes |
| `packages/web/src/components/chat/ChatMessage.tsx` | Import `SpinnerBlock`; replace tool call status spinners |
| `packages/web/src/styles/index.css` | Add `mcode-input-glow` class; remove dead `.animate-spin-slow` |

### Verification
- TypeScript compiles with 0 errors
- All animation components render correctly in both Chat tab and AI Code Editor tab
- No dead CSS classes remain
- `--mcode-*` CSS variables at `:root` level (already verified)